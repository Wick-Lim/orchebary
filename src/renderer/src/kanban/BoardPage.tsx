/**
 * Kanban board root. Implemented in milestone M6 — this placeholder keeps
 * the app shell compiling while subsystems land in parallel.
 */
export function BoardPage(): React.JSX.Element {
  return (
    <div style={{ flex: 1, display: 'grid', placeItems: 'center', color: 'var(--fg-muted)' }}>
      Board coming online…
    </div>
  )
}
