/**
 * Agent pipeline IPC: runs:*, git:*, worktree:*, agents:* channels.
 * Implemented in milestone M7 — this stub keeps main/index.ts wiring stable.
 */
export function registerAgentIpc(): void {
  // filled in by the agent pipeline subsystem
}
