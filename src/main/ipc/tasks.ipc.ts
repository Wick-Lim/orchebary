/**
 * Kanban IPC: projects:* and tasks:* channels.
 * Implemented in milestone M6 — this stub keeps main/index.ts wiring stable.
 */
export function registerTasksIpc(): void {
  // filled in by the kanban subsystem
}
